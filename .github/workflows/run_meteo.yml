name: Aggiorna Mappe Meteo

on:
  schedule:
    - cron: '0 */3 * * *' # Gira ogni 3 ore in automatico
  workflow_dispatch: # Ti permette di farlo partire anche a mano con un click quando vuoi

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4
        
      - name: Conferma sistema attivo
        run: echo "Il sistema di generazione mappe è pronto e configurato!"
